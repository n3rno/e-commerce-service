<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.hhplus.be.server.goods.infrastructure.persistence.mapper.GoodsMapper">

    <!-- 상품 정보 조회 (남은 재고 포함) -->
    <select id="selectGoodsByGoodsNo" parameterType="long" resultType="kr.hhplus.be.server.goods.domain.model.GoodsResponseDto">
        WITH sold_goods AS (
            SELECT goods_no, IFNULL(SUM(quantity), 0) AS sold_stock
            FROM order_goods
            WHERE goods_no = #{goodsNo}
            GROUP BY goods_no
        )
        SELECT
            g.no,
            g.name,
            g.price,
            g.stock,
            g.stock - IFNULL(sg.sold_stock, 0) AS remain_stock
        FROM goods g
        LEFT JOIN sold_goods sg ON g.no = sg.goods_no
        <where>
            g.no = #{goodsNo}
        </where>
    </select>
    
    <select id="selectGoodsByGoodsList" parameterType="list" resultType="kr.hhplus.be.server.goods.domain.model.GoodsResponseDto">
        WITH sold_goods AS (
            SELECT goods_no, IFNULL(SUM(quantity), 0) AS sold_stock
            FROM order_goods
            <where>
                goods_no IN
                <foreach collection="goodsList" item="goods" open="(" separator="," close=")">
                    #{goods.goodsNo}
                </foreach>
            </where>
            GROUP BY goods_no
        )
        SELECT
            g.no,
            g.name,
            g.price,
            g.stock,
            g.stock - IFNULL(sg.sold_stock, 0) AS remain_stock
        FROM goods g
        LEFT JOIN sold_goods sg ON g.no = sg.goods_no
        <where>
            g.no IN
            <foreach collection="goodsList" item="goods" open="(" separator="," close=")">
                #{goods.goodsNo}
            </foreach>
        </where>
    </select>

    <!-- 상품 재고 차감 -->
    <!--
        조건부 UPDATE 단일 쿼리
            UPDATE 문 자체가 원자 연산이므로 다른 트랜잭션이 끼어들 수 없음.
            영향 행 수 0 → 품절 처리. 영향 행 수 1 → 구매 성공.
            장점: 코드 간결, DB 하나로 충분.
            단점: 인기 상품 하나에 트래픽 집중 시 DB 핫스팟 가능.
     -->
    <update id="updateGoodsDecreaseStock" parameterType="int">
        UPDATE goods
        SET stock = stock - #{quantity}
        WHERE no = #{goodsNo}
          AND stock >= #{quantity}
    </update>
    
</mapper>